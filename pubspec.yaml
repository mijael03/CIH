name: cih
description: Code Intelligence Harness — inteligencia de código estructurada para agentes de IA.
publish_to: "none"
version: 0.0.1

environment:
  sdk: ^3.11.0
dependencies:
  analyzer: ^13.3.0
  path: ^1.9.1
dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.1
